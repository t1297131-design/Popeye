services:
  console-web:
    build: .
    image: console-web-starter:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
    volumes:
      - app-data:/app/data

volumes:
  app-data:
